[alias]
	aliases = config --get-regexp alias
	cam = commit -am
	pcb = push -u origin HEAD
	latest = pull --all --tags
	new = checkout -b
	del = branch -D
	cleanup = !git checkout main && git branch | grep -v "main" | xargs git branch -D
[user]
	name = YourName
	email = example@example.com
[pull]
	rebase = false
[fetch]
	prune = true